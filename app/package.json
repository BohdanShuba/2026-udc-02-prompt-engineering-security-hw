{
  "name": "udc-ws02-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tiny TS sample — a target for the prompt cookbook (tests/review/refactor/docs).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
